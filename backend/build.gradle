dependencies {
    compile project(':backend:controller'), project(':backend:dao'),
            project(':backend:domain'), project(':backend:dto'),
            project(':backend:service'), project(':backend:utils')
}